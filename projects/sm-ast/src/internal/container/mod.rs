use crate::{SMResult, AST};

pub fn range(expr: &AST) -> SMResult<AST> {
    match expr {
        _ => unimplemented!(),
    }
}

pub fn power_range(expr: &AST) -> SMResult<AST> {
    match expr {
        _ => unimplemented!(),
    }
}
