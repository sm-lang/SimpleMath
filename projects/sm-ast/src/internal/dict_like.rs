use crate::{SMResult, AST};

pub fn keys(expr: &AST) -> SMResult<AST> {
    match expr {
        _ => unimplemented!(),
    }
}

pub fn values(expr: &AST) -> SMResult<AST> {
    match expr {
        _ => unimplemented!(),
    }
}
